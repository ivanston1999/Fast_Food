package com.android.fastfood.presentation.detailactivity

import android.os.Bundle
import androidx.lifecycle.ViewModel
import com.android.fastfood.model.FoodMenu

class DetailViewModel (private val extras: Bundle): ViewModel() {
//    val food = extras?.getParcelable<FoodMenu>(DetailActivity.FOOD_DETAIL)
}